import "./App.css";

const App = () => {
  const onApiFetch = () => {};

  return (
    <div className="app">
      <h1>Fetch API Ext.</h1>
      <button onClick={onApiFetch}>Fetch data</button>
    </div>
  );
};

export default App;
